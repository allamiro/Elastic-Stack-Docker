# Kafka SSL Client Configuration
# This file is used by Kafka clients to connect to the SSL-enabled Kafka cluster

security.protocol=SSL
ssl.truststore.location=/opt/kafka/config/certs/ca/ca.p12
ssl.truststore.password=changeme
ssl.truststore.type=PKCS12

ssl.keystore.location=/opt/kafka/config/certs/kafka-broker-1/kafka-broker-1.p12
ssl.keystore.password=changeme
ssl.keystore.type=PKCS12
ssl.key.password=changeme

# Disable hostname verification for internal communication
ssl.endpoint.identification.algorithm=
